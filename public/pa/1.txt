#ja som 2 rocnik a teraz len sa 
#snazim to naucit aj python 


fileUrl="http://people.tuke.sk/peter.butka/res/solutions.csv" 
download.file(fileUrl, destfile="solutions.csv")

x = c(2,1,3,5,9,5,3)
xn=(x-1)/8
print(xn)

dfsolutions=read.csv("solutions.csv")
time=dfsolutions$time_left
head(dfsolutions,10)

str(dfsolutions)
dfsolutions=na.omit(dfsolutions)
dfsolutions$subject_id = as.factor(dfsolutions$subject_id)
dfsolutions[6,"stop"]
dfsolutions[6,4]

library(dplyr)
dfsolutions_numeric = select_if(dfsolutions, is.numeric)

lapply(dfsolutions_numeric, mean)
dfsolutions$time = ifelse(dfsolutions$time_left > 2200, TRUE, FALSE)
dfsolutions_1 = filter(dfsolutions,dfsolutions$problem_id > 200)
dfsolutions_2 = arrange(dfsolutions, dfsolutions$subject_id)
summary(dfsolutions$time_left)

boxplot(dfsolutions$time_left ~ dfsolutions$answer, col = "red")
hist((filter(dfsolutions, dfsolutions$subject_id == 23))$time_left, col = "green", abline(h=5, lwd=5, lty=2)

library(lattice)
xyplot(start ~ stop | answer, data=dfsolutions, layout=c(5,1))

library(ggplot2)
qplot(start, stop, data=dfsolutions) + facet_wrap(~time)


funkcia = function(c,d,n) { f=seq(1, n, by=0.5)
for (i in 1:length(f)){
if (f[i] == c){ print("A")
}
else if (f[i] == d){
print("B") }
        else print(f[i])
    }
}
funkcia(3,6,10)

data=quakes
par(mfrow=c(3,1))
hist(data$mag, col="blue", main="Histogram of quake magnitudes", breaks=15,
xlab="Magnitude of quakes", ylab="Number of quakes") abline(v=4.8, lwd=3, col="red", lty=2)
boxplot(data$depth, ylab="Depth", main="Depth of quakes", col="green") abline(h=400, col="yellow", lwd=5)
plot(lat ~ long, data=data, col="red", pch=2, xlab="Longitude", ylab="Latitude", m rect(172,-30,182,-20, lwd=3, border="blue")
